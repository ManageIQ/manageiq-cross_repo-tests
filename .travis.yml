dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20216,manageiq-api#843
  matrix:
  - TEST_REPO=manageiq#20216
  - TEST_REPO=manageiq-ui-classic
  - TEST_REPO=manageiq-api#843
  - TEST_REPO=manageiq-api-client
  - TEST_REPO=manageiq-automation_engine
  - TEST_REPO=manageiq-content
