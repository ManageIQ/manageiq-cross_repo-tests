dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20190,manageiq-ui-classic#7058,manageiq-schema#487
  matrix:
  - TEST_REPO=manageiq#20190
  - TEST_REPO=manageiq-schema#487
  - TEST_REPO=manageiq-ui-classic#7058
