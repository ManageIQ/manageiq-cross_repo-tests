---
language: ruby
rvm:
- 2.6.6
cache:
  directories:
  - vendor/bundle
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
branches:
  only:
  - master
env:
  global:
  - REPOS=ManageIQ/azure-armrest@master,ManageIQ/manageiq-providers-azure#465
  matrix:
  - TEST_REPO=ManageIQ/azure-armrest@master
  - TEST_REPO=ManageIQ/manageiq-providers-azure#465
