dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#19949,manageiq-ui-classic#6782,manageiq-schema#464
  matrix:
  - TEST_REPO=manageiq-api
  - TEST_REPO=manageiq-providers-foreman
  - TEST_REPO=manageiq#19949
  - TEST_REPO=manageiq-schema#464
  - TEST_REPO=manageiq-ui-classic#6782
