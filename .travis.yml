dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20192,manageiq-ui-classic#7049,manageiq-schema#486
  matrix:
  - TEST_REPO=manageiq#20192
  - TEST_REPO=manageiq-schema#486
  - TEST_REPO=manageiq-ui-classic#7049
