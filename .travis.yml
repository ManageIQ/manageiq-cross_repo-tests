dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#19684,manageiq-providers-amazon#584,manageiq-providers-azure#374,manageiq-providers-google#119,manageiq-providers-kubernetes#353,manageiq-providers-openstack#547,manageiq-providers-openshift,manageiq-providers-ovirt#454,manageiq-providers-scvmm,manageiq-providers-vmware#508
  matrix:
  - TEST_REPO=manageiq#19684
  - TEST_REPO=manageiq-providers-amazon#584
  - TEST_REPO=manageiq-providers-azure#374
  - TEST_REPO=manageiq-providers-google#119
  - TEST_REPO=manageiq-providers-kubernetes#353
  - TEST_REPO=manageiq-providers-openstack#547
  - TEST_REPO=manageiq-providers-openshift
  - TEST_REPO=manageiq-providers-ovirt#454
  - TEST_REPO=manageiq-providers-scvmm
  - TEST_REPO=manageiq-providers-vmware#508
