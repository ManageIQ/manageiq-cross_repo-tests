dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20229,manageiq-providers-amazon#627,manageiq-providers-azure#398,manageiq-providers-azure_stack#33,manageiq-providers-google#138,manageiq-providers-openstack#603,manageiq-providers-ovirt#498,manageiq-providers-vmware#585
  matrix:
  - TEST_REPO=manageiq#20229
  - TEST_REPO=manageiq-providers-amazon#627
  - TEST_REPO=manageiq-providers-azure#398
  - TEST_REPO=manageiq-providers-azure_stack#33
  - TEST_REPO=manageiq-providers-google#138
  - TEST_REPO=manageiq-providers-nsxt
  - TEST_REPO=manageiq-providers-nuage#215
  - TEST_REPO=manageiq-providers-openstack#603
  - TEST_REPO=manageiq-providers-ovirt#498
  - TEST_REPO=manageiq-providers-vmware#585
