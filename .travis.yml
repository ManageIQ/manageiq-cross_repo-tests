dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20018,manageiq-ui-classic#6802
  matrix:
  - TEST_REPO=manageiq#20018
  - TEST_REPO=manageiq-api
  - TEST_REPO=manageiq-providers-nuage
  - TEST_REPO=manageiq-ui-classic#6802
