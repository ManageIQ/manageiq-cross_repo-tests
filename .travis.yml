dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20246
  matrix:
  - TEST_REPO=manageiq#20246
  - TEST_REPO=manageiq-api#855
  - TEST_REPO=manageiq-content#660
  - TEST_REPO=manageiq-providers-ovirt#499
  - TEST_REPO=manageiq-providers-openstack#605
  - TEST_REPO=manageiq-providers-vmware#587
  - TEST_REPO=manageiq-schema#490
