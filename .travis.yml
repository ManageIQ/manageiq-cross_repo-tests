dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=d-m-u/ansible_tower_client_ruby@adding_saner_limits_to_faraday_version d-m-u/manageiq-api-client@updating_faraday
  matrix:
  - TEST_REPO=manageiq-providers-ansible_tower
