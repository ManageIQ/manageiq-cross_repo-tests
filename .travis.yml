dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#19607,manageiq-providers-amazon#581,manageiq-providers-azure#373,manageiq-providers-openstack#543,manageiq-providers-ovirt#446,manageiq-providers-scvmm#153,manageiq-providers-vmware#491
  matrix:
  - TEST_REPO=manageiq#19607
  - TEST_REPO=manageiq-providers-amazon#581
  - TEST_REPO=manageiq-providers-azure#373
  - TEST_REPO=manageiq-providers-openstack#543
  - TEST_REPO=manageiq-providers-ovirt#446
  - TEST_REPO=manageiq-providers-scvmm#153
  - TEST_REPO=manageiq-providers-vmware#491
  - TEST_REPO=manageiq-api
  - TEST_REPO=manageiq-ui-classic
  - TEST_REPO=manageiq-v2v
