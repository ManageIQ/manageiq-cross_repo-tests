dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20438,manageiq-ui-classic#7259,manageiq-schema#509
  matrix:
  - TEST_REPO=manageiq#20438
  - TEST_REPO=manageiq-schema#509
  - TEST_REPO=manageiq-ui-classic#7259
