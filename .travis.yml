dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20289
  matrix:
  - TEST_REPO=manageiq#20289
  - TEST_REPO=manageiq-providers-amazon
  - TEST_REPO=manageiq-providers-azure#416
  - TEST_REPO=manageiq-providers-kubernetes
  - TEST_REPO=manageiq-providers-openshift
  - TEST_REPO=manageiq-providers-openstack
  - TEST_REPO=manageiq-providers-vmware
