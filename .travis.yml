dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=https://github.com/ManageIQ/manageiq-providers-openshift/pull/165
  matrix:
  - TEST_REPO=https://github.com/ManageIQ/manageiq-providers-kubernetes/pull/365
