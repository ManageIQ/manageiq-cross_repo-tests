dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - TEST_REPO=https://github.com/ManageIQ/manageiq/pull/19477
  - GEM_REPOS=https://github.com/ManageIQ/manageiq-providers-kubernetes/pull/346,https://github.com/ManageIQ/manageiq-providers-openshift/pull/152
