dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=https://github.com/ManageIQ/manageiq/pull/19875
  matrix:
  - TEST_REPO=https://github.com/ManageIQ/manageiq-providers-vmware/pull/542
