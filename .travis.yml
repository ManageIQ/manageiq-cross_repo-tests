dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#19673,manageiq-providers-vmware#506,manageiq-ui-classic#6576
  matrix:
  - TEST_REPO=manageiq#19673
  - TEST_REPO=manageiq-providers-vmware#506
  - TEST_REPO=manageiq-ui-classic#6576
  - TEST_REPO=manageiq-api
  - TEST_REPO=manageiq-automation_engine
