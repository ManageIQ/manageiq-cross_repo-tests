dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
    - REPOS=manageiq#20156,manageiq-ui-classic#7039,manageiq-schema#480,manageiq-schema#481
  matrix:
    - TEST_REPO=manageiq#20156
    - TEST_REPO=manageiq-schema#480
    - TEST_REPO=manageiq-schema#481
    - TEST_REPO=manageiq-ui-classic#7039
