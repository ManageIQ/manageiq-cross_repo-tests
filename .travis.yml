dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq-schema#326,manageiq#19617
  matrix:
  - TEST_REPO=manageiq#19617
  - TEST_REPO=manageiq-ui-classic#6514
  - TEST_REPO=manageiq-providers-vmware#492
  - TEST_REPO=manageiq-providers-scvmm
  - TEST_REPO=manageiq-providers-ovirt#447
