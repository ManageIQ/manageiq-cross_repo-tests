---
language: ruby
dist: bionic
rvm:
- 2.6.6
cache:
  directories:
  - vendor/bundle
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
branches:
  only:
  - master
env:
  global:
  - REPOS=ManageIQ/inventory_refresh#97,ManageIQ/manageiq#21531
  matrix:
  - TEST_REPO=ManageIQ/manageiq#21531
  - TEST_REPO=ManageIQ/manageiq-api
  - TEST_REPO=ManageIQ/manageiq-automation_engine
  - TEST_REPO=ManageIQ/manageiq-consumption
  - TEST_REPO=ManageIQ/manageiq-content
  - TEST_REPO=ManageIQ/manageiq-decorators
  - TEST_REPO=ManageIQ/manageiq-gems-pending
  - TEST_REPO=ManageIQ/manageiq-schema
  - TEST_REPO=ManageIQ/manageiq-ui-classic
  - TEST_REPO=ManageIQ/manageiq-ui-service
