dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq@jansa,manageiq-providers-openshift#166,manageiq-providers-kubernetes#367
  matrix:
  - TEST_REPO=manageiq-providers-openshift#166
  - TEST_REPO=manageiq-providers-kubernetes#367
