dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20694
  matrix:
  - TEST_REPO=manageiq#20694
  - TEST_REPO=manageiq-providers-openstack
  - TEST_REPO=manageiq-providers-ovirt
  - TEST_REPO=manageiq-providers-scvmm
  - TEST_REPO=manageiq-providers-vmware
