---
language: ruby
rvm:
- 2.5.5
cache: bundler
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
branches:
  only:
  - master
env:
  global:
  - REPOS=ManageIQ/manageiq-providers-kubernetes#423,ManageIQ/manageiq-providers-openshift#204
  matrix:
  - TEST_REPO=ManageIQ/manageiq-providers-kubernetes#423
  - TEST_REPO=ManageIQ/manageiq-providers-openshift#204
