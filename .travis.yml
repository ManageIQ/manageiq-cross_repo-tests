dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20246
  - REPOS=manageiq-api#855
  - REPOS=manageiq-content#660
  - REPOS=manageiq-providers-openstack#605
  - REPOS=manageiq-providers-ovirt#499
  - REPOS=manageiq-providers-vmware#587
  - REPOS=manageiq-schema#490
  matrix:
  - TEST_REPO=manageiq-api
  - TEST_REPO=manageiq-content
  - TEST_REPO=manageiq-providers-openstack
  - TEST_REPO=manageiq-providers-ovirt
  - TEST_REPO=manageiq-providers-vmware
  - TEST_REPO=manageiq-schema
