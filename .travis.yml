dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - CORE_REPO=manageiq#19436
  - GEM_REPOS=agrare/manageiq-providers-amazon@optionally_symlink_core_repo,manageiq-gems-pending#451
  matrix:
  - TEST_REPO=manageiq#19436
  - TEST_REPO=agrare/manageiq-providers-amazon@optionally_symlink_core_repo
  - TEST_REPO=manageiq-gems-pending#451
