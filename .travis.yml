dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - CORE_REPO=manageiq#19452
  - GEM_REPOS=manageiq-providers-amazon#571,manageiq-providers-azure#361,manageiq-providers-google#113,manageiq-providers-ovirt#422,manageiq-providers-scvmm#139,manageiq-providers-vmware#470
  matrix:
  - TEST_REPO=manageiq#19452
  - TEST_REPO=manageiq-providers-amazon#571
  - TEST_REPO=manageiq-providers-azure#361
  - TEST_REPO=manageiq-providers-google#113
  - TEST_REPO=manageiq-providers-ovirt#422
  - TEST_REPO=manageiq-providers-scvmm#139
  - TEST_REPO=manageiq-providers-vmware#470
