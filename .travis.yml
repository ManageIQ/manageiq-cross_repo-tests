---
language: ruby
rvm:
- 2.6.6
cache:
  directories:
  - vendor/bundle
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
branches:
  only:
  - master
env:
  global:
  - REPOS=ManageIQ/manageiq-providers-ibm_cloud#268,agrare/manageiq@morphy_add_cloud_volume_types_inventory_collection
  matrix:
  - TEST_REPO=ManageIQ/manageiq-providers-ibm_cloud#268
  - TEST_REPO=agrare/manageiq@morphy_add_cloud_volume_types_inventory_collection
