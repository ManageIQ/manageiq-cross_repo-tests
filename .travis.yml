---
language: ruby
rvm:
- 2.6.6
cache:
  directories:
  - vendor/bundle
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
branches:
  only:
  - master
env:
  global:
  - REPOS=ManageIQ/manageiq-schema#580
  matrix:
  - TEST_REPO=ManageIQ/manageiq
  - TEST_REPO=ManageIQ/manageiq-schema#580
  - TEST_REPO=ManageIQ/manageiq-ui-classic
