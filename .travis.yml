dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#19824,manageiq-schema#455,manageiq-automation_engine#425
  matrix:
  - TEST_REPO=manageiq#19824
  - TEST_REPO=manageiq-schema#455
  - TEST_REPO=manageiq-automation_engine#425
  - TEST_REPO=manageiq-ui-classic
  - TEST_REPO=manageiq-api
