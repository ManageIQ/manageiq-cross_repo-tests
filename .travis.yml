dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq-providers-kubernetes#362,manageiq-providers-openshift#161
  matrix:
  - TEST_REPO=manageiq-providers-kubernetes#362
  - TEST_REPO=manageiq-providers-openshift#161
