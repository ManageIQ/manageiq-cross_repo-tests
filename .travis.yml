dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq-schema#485,manageiq#20213
  matrix:
  - TEST_REPO=manageiq#20213
  - TEST_REPO=manageiq-providers-openshift
  - TEST_REPO=manageiq-providers-kubernetes
  - TEST_REPO=manageiq-ui-classic
  - TEST_REPO=manageiq-api