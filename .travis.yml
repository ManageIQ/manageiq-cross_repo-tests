dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=
  - REPOS=manageiq#20173,manageiq-providers-azure#395,manageiq-smartstate#128
  matrix:
  - TEST_REPO=manageiq#20173
  - TEST_REPO=manageiq-providers-azure#395
  - TEST_REPO=manageiq-smartstate#128