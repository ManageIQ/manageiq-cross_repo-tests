dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20567,manageiq-providers-amazon#651,manageiq-providers-openstack#638,manageiq-ui-classic#7323,manageiq-schema#514
  matrix:
  - TEST_REPO=manageiq#20567
  - TEST_REPO=manageiq-providers-amazon#651
  - TEST_REPO=manageiq-providers-openstack#638
  - TEST_REPO=manageiq-ui-classic#7323
  - TEST_REPO=manageiq-providers-kubernetes#400
