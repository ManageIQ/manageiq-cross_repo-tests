dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20318,manageiq-providers-foreman#68,manageiq-providers-ansible_tower#234
  matrix:
  - TEST_REPO=manageiq#20318
  - TEST_REPO=manageiq-providers-foreman#68
  - TEST_REPO=manageiq-providers-ansible_tower#234
  - TEST_REPO=manageiq-providers-openstack
