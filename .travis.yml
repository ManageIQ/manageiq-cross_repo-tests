language: ruby
rvm:
- 2.5.5
cache: bundler
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
branches:
  only:
  - master
env:
  global:
  - REPOS=manageiq#20927,manageiq-providers-openstack#673,manageiq-schema#543
  matrix:
  - TEST_REPO=manageiq#20927
  - TEST_REPO=manageiq-providers-openstack#673
  - TEST_REPO=manageiq-ui-classic
  - TEST_REPO=manageiq-api
