dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - CORE_REPO=https://github.com/ManageIQ/manageiq/pull/19536
  - GEM_REPOS=https://github.com/ManageIQ/manageiq-schema/pull/437,https://github.com/ManageIQ/manageiq-providers-vmware/pull/482,https://github.com/ManageIQ/manageiq-providers-amazon/pull/579
  matrix:
  - TEST_REPO=https://github.com/ManageIQ/manageiq/pull/19536
  - TEST_REPO=https://github.com/ManageIQ/manageiq-providers-vmware/pull/482
  - TEST_REPO=https://github.com/ManageIQ/manageiq-providers-amazon/pull/579
  - TEST_REPO=manageiq-ui-classic
  - TEST_REPO=manageiq-providers-ansible_tower
  - TEST_REPO=manageiq-providers-azure
  - TEST_REPO=manageiq-providers-foreman
  - TEST_REPO=manageiq-providers-google
  - TEST_REPO=manageiq-providers-kubernetes
  - TEST_REPO=manageiq-providers-kubevirt
  - TEST_REPO=manageiq-providers-lenovo
  - TEST_REPO=manageiq-providers-openshift
  - TEST_REPO=manageiq-providers-openstack
  - TEST_REPO=manageiq-providers-ovirt
  - TEST_REPO=manageiq-providers-redfish
  - TEST_REPO=manageiq-providers-scvmm
