dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - TEST_REPO=manageiq-providers-vmware
  - CORE_REPO=manageiq@19401
