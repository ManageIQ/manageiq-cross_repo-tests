dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#20246,manageiq-api#855,manageiq-content#660,manageiq-providers-openstack#605,manageiq-providers-ovirt#499,manageiq-providers-vmware#587,manageiq-schema#490
  matrix:
  - TEST_REPO=manageiq-api
  - TEST_REPO=manageiq-content
  - TEST_REPO=manageiq-providers-openstack
  - TEST_REPO=manageiq-providers-ovirt
  - TEST_REPO=manageiq-providers-vmware
  - TEST_REPO=manageiq-schema
