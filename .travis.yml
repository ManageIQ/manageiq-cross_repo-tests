---
language: ruby
dist: bionic
rvm:
- 2.6.6
cache:
  directories:
  - vendor/bundle
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
branches:
  only:
  - master
env:
  global:
  - REPOS=ManageIQ/manageiq-providers-openstack#762
  matrix:
  - TEST_REPO=ManageIQ/manageiq-providers-ibm_cic
  - TEST_REPO=ManageIQ/manageiq-providers-ibm_power_vc
  - TEST_REPO=ManageIQ/manageiq-providers-openstack#762
