dist: xenial
language: ruby
rvm:
- 2.5.5
cache:
  bundler: true
addons:
  postgresql: '10'
  apt:
    packages:
    - libarchive-dev
script: bundle exec manageiq-cross_repo
matrix:
  fast_finish: true
env:
  global:
  - REPOS=manageiq#19534,manageiq-schema#436,manageiq-providers-vmware#481,manageiq-providers-ovirt#440,manageiq-providers-scvmm#151
  matrix:
  - TEST_REPO=manageiq#19534
  - TEST_REPO=manageiq-providers-vmware#481
  - TEST_REPO=manageiq-providers-ovirt#440
  - TEST_REPO=manageiq-providers-scvmm#151
  - TEST_REPO=manageiq-ui-classic
  - TEST_REPO=manageiq-api
